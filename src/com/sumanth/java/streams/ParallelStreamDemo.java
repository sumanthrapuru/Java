package com.sumanth.java.streams;

public class ParallelStreamDemo {
    public static void main(String[] args) {
        //merge sort quick sort can be parallelized
        //inherently sequential operations can't be parallelized
        //STATELESS state of one element doesn't affect another element
        //ASSOCIATIVE result not affected by other operands
        //NONINTERFERING source data not affected
        //Use when you have
        //1)large data sets
        //2)performance issues
        //3)shared resources must be synchronized properly


    }
}
